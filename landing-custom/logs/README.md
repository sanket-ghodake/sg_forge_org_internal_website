# Custom Landing Logs Directory

This directory stores structured logs and operational telemetry for the custom landing service.

## Governance
- Runtime log files (`*.log`) are ephemeral and gitignored.
- Structured JSON log entries conform to Google SRE standards.
