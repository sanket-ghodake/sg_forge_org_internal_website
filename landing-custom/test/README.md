# Custom Landing Tests

Tier 1 Unit test suites for the custom landing starter base.

## Test Files
- `landing-custom.test.ts`: Healthcheck, root landing HTML rendering, config overrides, and 404 response handling.
