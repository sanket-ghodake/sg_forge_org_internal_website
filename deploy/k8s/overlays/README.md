# Kubernetes Environment Overlays

Environment-specific Kustomize overlays for SG Forge.

## Environments
- `dev/`: Development environment overlay (single replica, dev namespace).
- `prod/`: Production environment overlay (high-availability, production namespace).
