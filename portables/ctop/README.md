# Portable ctop (Container Top Terminal Dashboard)

Precompiled standalone binary for `ctop` (v0.7.7) for real-time container metrics (CPU, memory, network, and disk I/O).

## Usage
Invoked via the repo wrapper:
```bash
./portables/bin/ctop
# or
./run.sh top
```
