# ==============================================================================
# SG Forge 2.0 - Production Docker Environment
# ==============================================================================
name: sg-forge-prod

networks:
  forge-net:
    driver: bridge

volumes:
  forge_caddy_data:
  forge_caddy_config:
  forge_prod_db_auth:
  forge_prod_db_portal:
  forge_prod_db_dev_dashboard:
  forge_prod_db_expenses:
  forge_prod_db_billing:
  forge_prod_db_telemetry:

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  # 🔀 Unified Reverse Proxy Gateway
  proxy:
    image: caddy:2-alpine
    container_name: forge-proxy-prod
    profiles: ["core", "apps", "monitoring", "all"]
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
    volumes:
      - ../../proxy/Caddyfile:/etc/caddy/Caddyfile:ro
      - forge_caddy_data:/data
      - forge_caddy_config:/config
    networks:
      - forge-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:80/ || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.50'

  # 🌐 1. Platform Landing & Discovery Hub (Port 3000)
  landing:
    build:
      context: ../../
      dockerfile: apps/src/landing/docker/Dockerfile
    container_name: forge-landing-prod
    profiles: ["core", "all"]
    env_file:
      - ../../.env
    environment:
      - PORT=${LANDING_PORT:-3000}
      - LANDING_PORT=${LANDING_PORT:-3000}
      - NODE_ENV=production
    networks:
      - forge-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '1.0'

  # 🏢 2. Main Portal & Org Canvas (Port 3001)
  portal:
    build:
      context: ../../
      dockerfile: apps/src/portal/docker/Dockerfile
    container_name: forge-portal-prod
    profiles: ["core", "all"]
    env_file:
      - ../../.env
    environment:
      - PORT=${PORTAL_PORT:-3001}
      - PORTAL_PORT=${PORTAL_PORT:-3001}
      - NODE_ENV=production
    volumes:
      - forge_prod_db_portal:/app/data
    networks:
      - forge-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3001/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '1.0'

  # 📊 3. Developer Monitoring Dashboard (Port 3002)
  dev-dashboard:
    build:
      context: ../../
      dockerfile: apps/src/dev-dashboard/docker/Dockerfile
    container_name: forge-dev-dashboard-prod
    profiles: ["monitoring", "all"]
    env_file:
      - ../../.env
    environment:
      - PORT=${DEV_DASHBOARD_PORT:-3002}
      - DEV_DASHBOARD_PORT=${DEV_DASHBOARD_PORT:-3002}
      - NODE_ENV=production
    volumes:
      - forge_prod_db_dev_dashboard:/app/data
    networks:
      - forge-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3002/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '1.0'

  # 📚 4. Developer Hub & SDK Playground (Port 3003)
  dev-hub:
    build:
      context: ../../
      dockerfile: apps/src/dev-hub/docker/Dockerfile
    container_name: forge-dev-hub-prod
    profiles: ["monitoring", "all"]
    env_file:
      - ../../.env
    environment:
      - PORT=${DEV_HUB_PORT:-3003}
      - DEV_HUB_PORT=${DEV_HUB_PORT:-3003}
      - NODE_ENV=production
    networks:
      - forge-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3003/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '1.0'

  # 🔐 5. Central Identity & Auth Service (Port 3004)
  auth:
    build:
      context: ../../
      dockerfile: apps/src/auth/docker/Dockerfile
    container_name: forge-auth-prod
    profiles: ["core", "all"]
    env_file:
      - ../../.env
    environment:
      - PORT=${AUTH_PORT:-3004}
      - AUTH_PORT=${AUTH_PORT:-3004}
      - NODE_ENV=production
    volumes:
      - forge_prod_db_auth:/app/data
    networks:
      - forge-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3004/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '1.0'

  # 🧩 6. Forge App: Expenses (Port 8085)
  app-expenses:
    build:
      context: ../../
      dockerfile: forge-apps/expenses/docker/Dockerfile
    container_name: forge-app-expenses-prod
    profiles: ["apps", "all"]
    env_file:
      - ../../.env
    environment:
      - PORT=8085
      - NODE_ENV=production
    volumes:
      - forge_prod_db_expenses:/app/data
    networks:
      - forge-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:8085/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '1.0'

  # 🧩 7. Forge App: Billing (Port 8086)
  app-billing:
    build:
      context: ../../
      dockerfile: forge-apps/billing/docker/Dockerfile
    container_name: forge-app-billing-prod
    profiles: ["apps", "all"]
    env_file:
      - ../../.env
    environment:
      - PORT=8086
      - NODE_ENV=production
    volumes:
      - forge_prod_db_billing:/app/data
    networks:
      - forge-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:8086/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '1.0'

  # 🧩 8. Forge App: Telemetry (Port 8087)
  app-telemetry:
    build:
      context: ../../
      dockerfile: forge-apps/telemetry/docker/Dockerfile
    container_name: forge-app-telemetry-prod
    profiles: ["apps", "all"]
    env_file:
      - ../../.env
    environment:
      - PORT=8087
      - NODE_ENV=production
    volumes:
      - forge_prod_db_telemetry:/app/data
    networks:
      - forge-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:8087/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging: *default-logging
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '1.0'
