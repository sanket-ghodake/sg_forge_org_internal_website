# 🐳 Docker Environments (`docker/`)

Container configurations and Compose files for local development and production deployments.

* **[`dev/`](file:///home/sanket/Desktop/Sanket/org_website_clone/docker/dev/)**: Development stack with sub-5ms `bun --watch` hot-reloading, named volume persistence, and 128MB RAM caps.
* **[`prod/`](file:///home/sanket/Desktop/Sanket/org_website_clone/docker/prod/)**: Production-hardened container stack.
